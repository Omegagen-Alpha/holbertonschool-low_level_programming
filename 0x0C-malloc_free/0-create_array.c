#include <stdio.h>
#include "holbeerton.h"

/**
 * *create_array - creates an array of characters and initializes it with a specific char
 * @size: the adress of memory
 * @C: 
 *
 * Return: Null if size = 0, and the array itself otherwise
 */

char *create_array(unsigned int size, char C)
{
	ar = malloc(size * sizeof(
}
